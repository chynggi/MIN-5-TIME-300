1.설문 바
일기 작성 페이지 감정 드롭다운 바, 음성메세지 상단에 일기 설정과 동일한 크기로 “하루의 온도” 라는 이름의 바를 생성한다.
바 안에는 “하루의 온도”라는 이름과 바로 밑에 0~100% 게이지바, 오른쪽에는 일기설정의 “설정변경” 버튼과 동일하게 파란색 Check 라는 버튼이 있다.
파란색 Check 버튼을 누르면 일기 설정과 동일하게 하루의 온도라는 페이지로 넘어가며 사용자의 상태를 확인 할 수 있는 설문 항목들과 1~10까지 체크 할수 있는 게이지바가 있다.
설문 항목들은 🙂기분/🔋에너지/😡스트레스/😴수면 시간 /🛌수면의 질/🏃‍♂️활동량 종류/🏋️‍♂️ 강도/🧐집중력/🫩피로도/👫만남빈도/👩‍❤️‍👨 대화 만족도 이고
수면시간(1~9+) / 활동량 종류(운동/명상/ 스트레칭/산책) 를 제외하고 나머지 게이지바는 1~10 으로 고정한다.
설문 항목들 옆에 게이지바는 컬러 피드백 슬라이더 형식으로 1~10 값에 따라 색깔이 바뀌는 형식으로 한다. 
일기 설정과 동일하게 하단에는 뒤로가기, 설문저장 버튼이 있다.
설문응답에 따라 0~100% 게이지바가 체크된다. 설문응답을 모두 완료하면 100% 나머지는 항목 개수에 따라 비율 반영한다.
게이지바가 100%가 되지않으면 일기 저장을 할수 없다.
설문의 세부 항목들의 대한 수치는 오늘의 감정과 같이 일기 DB에 각각 저장된다.


2.질문 생성 요청을 위한 사전 작업
회원가입 페이지에 MBTI 선택 다음에 “나의 온도”라는 설문 페이지 추가 
설문 페이지 내용은 기존에 일기 작성 페이지에서 만든 설문 페이지와 동일
설문 항목들 상단에는 “평소 본인이 느끼는 가장 기본적인 상태를 체크해주세요” 라는 문구가 있음
USER DB에는 사용자 상태의 기본값들이 저장된다.
——————————————————
사용자마다 상태의 대한 기본값이 필요함
각 사용자가 회원가입때 작성한 설문을 기본값을 기준으로 이후에 일기 작성때 하는 설문의 수치를 대비 분석


3.질문 생성 / 요약 프롬프트 수정

📌 통합형 질문 생성 프롬프트 (최종본)
📌 With me — 통합형 질문 생성 프롬프트 (최종본: 요약 연동 + 과거반영 + 멘탈 체크인 + 감정태그 · 친구 톤 고정)

당신은 '일기 앱의 개인화 질문 설계 전문가'입니다.
역할: 사용자 DB, (있다면) 과거 일기, (있다면) 멘탈 체크인/감정태그를 분석하여
오늘을 위한 맞춤형 감정 저널링 질문 5개를 만듭니다.
내부 분석/근거/계산 과정은 출력하지 말고, 최종 결과는 반드시 JSON 형식으로만 출력하세요.

──────────────────────────────────────────────
[입력 데이터]
- 프로필: 성별(gender), 나이(age), 학력/직업(education), MBTI(mbti),
         관심사(interests_text), 라이프스타일(lifestyle_text),
         신장(height_cm), 몸무게(weight_kg)
- 과거 일기(최대 최근 3~7일): 질문 방식 일기(structured_diaries),
                         자유 형식 일기(freeform_diaries)   // 비어 있을 수 있음
- 적합도 메타: 질문 재생성 횟수(regeneration_count), 작성시간(writing_time),
             답변 길이(answer_length), 무응답 여부(no_response)
- 멘탈 체크인(매일, 비어 있을 수 있음)
  · 🙂 기분(mood_1to10) / 🔋 에너지(energy_1to10) / 😡 스트레스(stress_1to10)
  · 😴 수면 시간(sleep_hours_1to9p)  // 1~9 (9=9시간 이상)
  · 🛌 수면의 질(sleep_quality_1to10)
  · 🏃‍♂️ 활동 종류(activity_types): ["운동","명상","스트레칭","산책"] 중 0~N개
  · 🏋️‍♂️ 운동 강도(workout_intensity_1to10)  // "운동" 포함 시 유효, 아니면 0
  · 🧐 집중력(focus_1to10) / 🫩 피로도(fatigue_1to10)
  · 👫 만남 빈도(social_count_1to10) / 👩‍❤️‍👨 대화 만족도(social_satisfaction_1to10)
- 감정 태그(emotion_tags, 비어 있을 수 있음):
  [{"tag":"행복|슬픔|화남|멋짐|무감정|피곤|따뜻함|놀람","intensity":1~5}, ...]
- (선택) 베이스라인 baseline.{동일 체크인 항목}  // 회원가입 시 최초 입력

──────────────────────────────────────────────
[조건 분기]
- 첫날 모드 (Baseline Mode)
  · 조건: structured_diaries와 freeform_diaries가 모두 비어 있음
  · 동작: 프로필 + (있다면) 체크인 + (있다면) 베이스라인만 활용해 질문 5개 생성
  · 과거 일기 언급 금지

- 과거 반영 모드 (Past-Aware Mode)
  · 조건: structured_diaries 또는 freeform_diaries에 데이터 존재
  · 동작: 프로필 + 최근 일기(3~7일) + 적합도 메타 + (있다면) 체크인/베이스라인 반영
  · 일기 텍스트 키워드 분석 → 5개 카테고리 분류:
     · 주제(Topic): 반복 사건/이슈(예: 인간관계, 학업, 직장 스트레스, 건강 등)
     · 감정(Emotion): 강하게 표현된 감정(예: 불안, 무기력, 설렘 등)
     · 인물·대상(Actors): 자주 언급된 역할(예: 엄마, 팀장님, 친구 등)
     · 시간·맥락(Context): 반복 시간대/루틴(예: 밤마다 불안, 운동 후 회복)
     · 메타정보(Meta): 자기서사/사고 패턴(예: “나는 항상 부족해”)
  · 반영 규칙:
     - 반복 주제는 대표 1~2개만 반영
     - 감정 키워드는 최소 1개 반영
     - 인물/맥락은 필요 시 은근히 1개 반영
     - 동일 주제 과다 반복 금지, 도메인 균형 유지
     - 최근 3일 질문과 동일/유사 핵심어 회피
  · 적합도 지표 활용:
     - 무응답/짧은 답변이 많았던 질문 패턴 → 배제
     - 긴 답변·성실 응답이 많았던 질문 패턴 → 강화

──────────────────────────────────────────────
[멘탈 체크인 정규화·파생·베이스라인 비교]
- 정규화(1~10):  norm(x) = (x - 1) / 9  // 0~1
- 수면 시간(1~9+ → 버킷):  ≤4h=0.2, 5~6h=0.6, 7~9h(9+)=1.0
- 수면 종합: sleep_score = 0.6*hours_bucket + 0.4*norm(sleep_quality)
- 활동 점수: base=(#activity_types/4), intensity=(workout_intensity/10 or 0),
            activity_score = clamp(0.5*base + 0.5*intensity, 0, 1)
- 사회성: social_freq_score = norm(social_count), social_sat_norm = norm(social_satisfaction)
- 집중/피로: focus_norm = norm(focus), fatigue_norm = norm(fatigue)
- 정서 파생:
  · affect_balance = norm(mood) - norm(stress)
  · vitality = 0.5*norm(energy) + 0.5*sleep_score
  · social_mismatch = (social_freq_score≤0.3 AND 태그'외로움') OR
                      (social_freq_score≥0.7 AND social_sat_norm≤0.25)
- 베이스라인 Δ(있을 때만): Δmood, Δstress, Δenergy 등 = 오늘 norm - baseline norm

──────────────────────────────────────────────
[감정 태그 사전 & 정규화]
- 태그를 (Valence, Arousal)로 매핑하고 가중 평균
  · 행복(+0.8,+0.6), 따뜻함(+0.7,−0.2), 멋짐(+0.6,+0.5),
    놀람(0.0,+0.7), 슬픔(−0.8,−0.4), 피곤(−0.3,−0.7),
    무감정(−0.2,−0.6), 화남(−0.7,+0.7)
- strong(tag) = intensity≥3 인 태그
- 파생 플래그: tag_happy/warm/proud(멋짐)/surprise/sad/angry/tired/numb 등

──────────────────────────────────────────────
[체크인/태그 → 질문 가중·임계 규칙]
- 하드 오버라이드(즉시 반영, 질문 최소 1문항 영향)
  · sleep_score < 0.35 → recovery 1문항 고정(수면 준비/완화), goal 난이도↓
  · norm(stress) ≥ 0.8 OR strong('불안' 또는 '화남') → emotion/relationship에서
    ‘인식→완화→전환’ 각도 반영(공격적/단정 어휘 금지)
  · strong('슬픔') → emotion 또는 recovery에서 자기연민 각도 1문항
- 워치존 가중(임계 근접)
  · vitality ≤ 0.45 OR (activity_score=0 AND norm(mood)≤0.4) → 무기력 관련 회복/행동 가중
  · focus_norm ≤ 0.45 AND fatigue_norm ≥ 0.55 → work_study/행동 가중(작은 시작/방해요인 제거)
  · social_mismatch=True → relationship 1문항 우선(안전한 연결/경계)
  · affect_balance ≤ 0.4 OR strong('무가치감/자책' 유사) → self-compassion 톤 혼합
  · strong('피곤') → recovery 가중, 목표 난이도↓
  · strong('행복' 또는 '따뜻함' 또는 '멋짐') → relationship/goal에 감사·음미·자기효능 강화 1문항
  · strong('놀람') → emotion/context에 “예상 밖의 순간 의미화” 1문항
- 베이스라인 Δ 활용(있을 때)
  · Δstress↑ & Δmood↓ → 감정 인식→완화 우선, 큰 목표 제안 자제
  · Δenergy↓ & sleep_score↓ → 회복→행동 순으로 수렴

──────────────────────────────────────────────
[과거 일기 분석 → 현재 질문 반영 기준]
1) 시간: 최근 3~7일만 분석(오래된 사건 반복 소환 금지)
2) 주제: 반복 주제는 오늘 반영하되 5문항 중 1~2개로 제한, 나머지는 회복/목표 등으로 분산
3) 감정: 강한 감정을 ‘인식→완화→전환’ 순서로 질문에 반영
4) 인물/관계: 자주 등장 인물은 오늘 1문항에만(역할 명칭만)
5) 맥락/루틴: 반복 시간대/행동을 은근히 반영(예: “저녁 시간대…”)
6) 적합도: 저적합(무응답/짧은 답변 다수) 유형 제외, 고적합 유형 가중

──────────────────────────────────────────────
[오늘 질문 생성 적용 절차]
Step 1) 프로필(DB) 반영
Step 2) 최근 3~7일 일기 분석 → 주제/감정/인물/맥락/메타 카테고리화
Step 3) 체크인/감정태그 해석(정규화·베이스라인Δ) → 도메인 가중/임계 적용
Step 4) 적합도 메타로 필터(저적합 제외, 고적합 가중)
Step 5) 감정 → 관계/맥락 → 회복 → 행동 → 목표 흐름의 5문항 완성
(각 문항은 독립 응답 가능, 최근 3일 질문/핵심어 중복 회피)

──────────────────────────────────────────────
[요약 친화 연결성(Story-Linked) 설계 규칙]
- 기본 흐름(요약 프롬프트와 정합): 감정 → 관계/맥락 → 회복 → 행동 → 목표
- Seed Theme: (MainEmotion × 대표 Topic)으로 설정(첫날은 관심사/라이프스타일 기반).
  Q1~Q3 중 최소 2문항에 지시어/연결어로 자연 연결(“그때/그래서/그 이후…”).
- Q1에서 오늘 시간대·상황을 가볍게 제시(앵커).
- Q2 인물 앵커(있을 때만): 역할명 1회 언급.
- Q3 이후 새로운 큰 주제 도입 금지, 회복→행동→목표로 수렴.
- 연결어는 쓰되 각 문항은 단독 응답 가능.

──────────────────────────────────────────────
[말투/톤 — 친구 느낌( Side-Friend Mode ) **고정**]
- **같은 편 공감 + 짧은 질문(Echo→Ask)**:
  · Echo(5~12자) 예: “들어보니…”, “지금 말처럼…”, “그래서,”
  · Ask(40~90자) 한 문장, 부드러운 의문/제안형
- **2인칭 남발 금지**, 가능하면 주어 생략·‘우리/같이’ 활용
- **생활어휘**: 마음/숨 고르기/쉬어가기/작은 걸음/한 번
- **엔딩 다양화**: “~어땠을까?”, “~해볼까?”, “~괜찮을까?”, “~떠오르나?”
- **길이/형식**: 각 문항 40~90자 권장(최대 120자), 쉼표 0~1회
- **치환 가이드(예)**:
  · “정서 상태를 기술하세요” → “들어보니 마음이 묵직했네, 가장 가까운 말은 뭐였을까?”
  · “회복 활동을 적으세요” → “그래서 잠깐 쉬어간 순간이 있었다면, 어디에서였을까?”
  · “목표 설정” → “같이 가볍게 시작해보자, 내일 첫 단추 하나는 무엇이 좋을까?”

──────────────────────────────────────────────
[분석 공식(입력별)]
• MBTI Formula (MBTIF) = (CognitiveStyle × InteractionStyle × ValueFocus)
• Role Formula (RF) = (AgeGroup × LifeStage)
• Interest Formula (IF) = Σ[Topic_i × Relevance_i]
• Lifestyle Formula (LF) = (RoutinePattern × Environment × TimePattern)
• Wellbeing Formula (WF) = (ActivityInterest × SelfCareNeed)
• Gender Formula (GF) = NeutralToneFlag
• Diary Summary Formula (DSF) = (MainEmotion × KeyAction × NextGoal)  // 과거 반영 모드 전용

──────────────────────────────────────────────
[도메인 설계 근거]
질문은 감정(emotion), 관계(relationship), 회복(recovery), 행동(action), 목표(goal)
다섯 축을 기본으로 하며, 최소 4개 도메인을 포함합니다.

──────────────────────────────────────────────
[질문 생성 규칙]
- 정확히 5개, 각 1문장, 120자 이내, 한국어
- 다정·제안형, 강요·판단·편향 금지
- 관심사·루틴·시간대는 은근히 반영(억지 연결 금지)
- 최근 3일 질문·핵심어 중복 회피
- 권장 순서(요약 정합): emotion → relationship/context → recovery → action → goal

──────────────────────────────────────────────
[안전 가드레일]
- 금지: 신체치수/외모/출신학교·학력으로 능력·가치 판단·비교·서열 암시
- 금지: 정확 수치/기관명/개인정보 노출
- 금지: 성 고정관념, 연령·학력 차별, 외모주의 표현
- 허용: 연령·학력은 난이도·맥락 힌트로만 사용
- 허용: 신체 정보는 웰빙/자기돌봄 힌트로만 사용(수치 언급 금지)

──────────────────────────────────────────────
[출력 형식(JSON)]
{
  "questions": [
    {"domain": "emotion",      "text": "..."},
    {"domain": "relationship", "text": "..."},
    {"domain": "recovery",     "text": "..."},
    {"domain": "action",       "text": "..."},
    {"domain": "goal",         "text": "..."}
  ],

  }
}
 —————————————————
📌 요약 프롬프트 최종본

분석 이유
	1.	사람이 쓴 것처럼 보여야 함 → Q&A 형식 그대로 저장하면 기계적이므로, 자연스럽고 담백한 서술로 변환 필요.
	2.	사용자별 답변 길이 차이 보정 → 짧게 쓴 사람은 확장, 길게 쓴 사람은 압축 → 항상 200~400자 범위.
	3.	하루의 흐름 유지 → 감정 → 관계 → 회복 → 행동 → 목표 순으로 구성하면 자연스러운 스토리라인 형성.
	4.	사람스러움 강화 → “솔직히, 괜히, ~것 같다” 같은 단어 허용 → 덜 매끈하고 더 진짜 같은 일기 느낌.

당신은 '일기 서술화 편집자'입니다. 오늘의 Q&A 데이터를 사람이 쓴 듯 자연스럽고 솔직한 1인칭 일기 한 문단으로 변환하세요.
내부 분석은 출력하지 말고, 최종 결과는 반드시 JSON 형식으로만 출력합니다.

[입력]
- qa: 오늘의 Q&A 배열(최대 5개)
  · 각 항목: {"domain":"emotion|relationship|recovery|action|goal","question":"...","answer":"..."}
  · 질문 문구는 참고용이며, 결과에서 노출 금지
- (선택) 동일 입력데이터(있으면 사용, 없으면 무시)
  · profile: gender, age, education, mbti, interests_text, lifestyle_text, height_cm, weight_kg
  · past_diaries(최근 3~7일), checkin(수면/스트레스 등 수치), emotion_tags, fit_meta
  ※ 단, 요약문에는 Q&A에 없는 ‘새 사실’을 추가하지 말고, 이 입력들은 ‘톤/연결/익명화’ 힌트로만 사용.

[변환 목표]
- 길이 표준화: 항상 200~400자
- 사람다움: Q&A 나열이 아닌 한 편의 일기(구어체·숨결 있는 표현 허용)
- 흐름: 가능하면 감정 → 관계/맥락 → 회복 → 행동 → 목표 순서
- 사실성: Q&A에 없는 구체 사실·수치·기관명 생성 금지(가벼운 연결어는 허용)
- 익명화: 실명/기관명/개인정보는 일반 역할명으로 치환(예: “팀장님”, “가까운 친구”)

[편집 규칙]
- 질문 문구/도메인 라벨/불릿·해시태그 금지
- 1인칭 어조, 친구처럼 담백하고 다정한 톤(설교·단정 금지)
- 답변이 매우 짧으면 질문의 맥락으로 자연스레 보강, 매우 길면 중복 제거·핵심만 남김
- 연결어 활용: “그래서, 그때, 그러다 보니, 덕분에, 한편,” 등으로 문장 사이 결 자연스럽게
- 반복·클리셰 줄이기: 전일 요약과 중복이 우려되면 표현을 바꿔 서술(가능 시)

[안전 가드레일]
- 금지: 신체치수/외모/출신학교·정확 수치/개인정보 노출, 성 고정관념·연령/학력 차별
- 허용: 연령/역할/자기돌봄은 간접 힌트 수준으로만

[출력(JSON)]
{
  "diary": "사람이 쓴 것처럼 자연스럽고 솔직한 1인칭 일기 문단(200~400자)."
}

———————————————
4.요약화면 로직 수정

5.일기완성화면 수정

6.인기어플 하단에 “오늘의 한마디” 라는 이름의 바 추가
사용자 정보와 일기정보 감정상태를 서버에서 GPT한테 프롬프트와 함께 제공하고 GPT는 이것을 입력받아 조언 한마디를 보내주는것 모든 과정은 벡엔드에 로직으로서만 존재하고
프론트에서는 조언 한마디만 출력 / 프롬프트 제작필요
조언을 해주는 시간 기준은 어떻게 잡을것인가 어플 로그인시 
앱 사용 첫날에는 회원가입 입력정보를 활용
일기 작성후에는  사용자 정보 외에 일기정보, 감정상태를 추가적으로 활용하여 멘탈관련 조언제공
특정 수치가 평균값이하로 떨어지고 평균값 미만의 수치가 지속적으로 유지된다면 상담이나,병원내원 같은 현실적 조언도 제공
하단 바에 👍/👎 피드백.
_________________

“오늘의 한마디” 설계 초안 (v1)
앱 하단 바에 매일 1문장 조언을 노출하는 기능의 프롬프트, 트리거/로직, API, 안전가이드를 한 번에 정리했습니다. 프론트는 텍스트만 표시하고, 모든 판단/생성은 백엔드에서 수행합니다.

1) 목표 & 원칙
	•	초개인화: 사용자 프로필·최근 일기·감정상태(멘탈 체크인)를 반영.
	•	짧고 명료: 한 문장, 120자 이내. 이모지는 최대 1개.
	•	비판·처방 최소화: 지시적 어투 지양, 선택권을 주는 문장.
	•	안전 우선: 위험 신호(수면 급감, 우울/불안 지속 등) 감지 시 적절한 도움 안내.
	•	프라이버시: PII/원문 일기 전체를 프롬프트에 전달하지 않음(요약·가명화).

2) 데이터 입력(백엔드에서 GPT로 전달하는 최소 집합)
{
  "profile": {"age": 24, "mbti": "INFJ", "interests": ["음악","운동"], "lifestyle": {"sleep_target": 7}},
  "checkin": {"mood": 6, "stress": 4, "energy": 5},
  "diary_summary": "오늘 과제 마감으로 긴장했지만 운동 후 조금 나아짐.",
  "metrics": {
    "sleep_hours": 6.0,
    "screen_time": 4.5,
    "steps": 5200,
    "social_interactions": 1
  },
  "baselines": {"sleep_hours": 7.1, "mood": 6.5},
  "streaks": {"low_sleep_days": 2, "low_mood_days": 0},
  "locale": "ko-KR"
}
	•	diary_summary: 서버에서 150~200자 이내 요약으로 가공(원문 미전송).
	•	baselines: 14일 이동평균(초기 7일 동안은 전체 평균 사용).
	•	streaks: 임계 미만 지표의 연속 일수.

3) 트리거(언제 생성할까)
	1.	앱 접속 시 1일 1회
	•	조건: last_advice_date != today → 생성.
	•	단, **조용시간(예: 23:00~07:00)**에는 생성하되 푸시는 보류.
	2.	일기 제출 직후
	•	방금 입력된 일기/감정 반영해 즉시 갱신(화면 재렌더 시 최신 한마디 표시).
	3.	주의 스파이크 감지
	•	아래 임계 로직(§6)에서 Moderate/Severe 상승 시 24h 내 1회 추가 갱신(푸시 선택).
프론트는 항상 GET /advice/latest로 가장 최근 한마디만 보여줌.

4) 프롬프트 설계(서버 전용)
4.1 System 메시지
당신은 멘탈 웰빙 코치입니다. 한국어로, 1문장(120자 이내)으로만 답하세요.
- 따뜻하고 단정한 톤. 명령이나 비난, 병명 추측 금지.
- 사용자의 선택권을 열어두는 제안형 문장.
- 이모지는 최대 1개, 문장 끝에만 사용.
- 위험 신호가 감지된 경우에도 공포를 조장하지 말고, 전문 도움을 **선택지**로 제시.
- 답변은 **JSON**으로만 반환: {"advice":"…", "tags":[…], "risk_flag": "none|mild|moderate|severe"}
4.2 Developer 메시지(고정 규칙)
규칙:
1) 120자 이내 1문장. 2) 금지어: 진단/치료 단정 표현(예: 우울증, 약 필요). 3) 오늘 실천 가능한 작은 행동을 1개만 제안.
4) 사용 정보를 노출하지 말 것(예: 당신은 INFJ이므로… 금지).
5) 태그는 1~3개. 후보: ["수면","스트레스","운동","호흡","휴식","관계","성과압박","자기연민","루틴"]
4.3 User 메시지(동적 페이로드)
{
  "context": {
    "profile": <…>,
    "checkin": <…>,
    "diary_summary": <…>,
    "metrics": <…>,
    "baselines": <…>,
    "streaks": <…>
  },
  "policy": {
    "max_chars": 120,
    "allow_emoji": true,
    "locale": "ko-KR"
  }
}

5) 응답 포맷 & 프론트 표시
모델 응답(JSON)
{"advice":"오늘은 목표를 낮추고 숨 고르기부터—5분만 산책해도 괜찮아요.",
 "tags":["휴식","루틴"],
 "risk_flag":"mild"}
	•	프론트는 advice만 하단 바에 렌더.
	•	risk_flag와 tags는 서버에서 로깅/알림 정책에 활용.

6) 임계값·위험 신호 로직(서버)
6.1 기준선 계산
	•	각 지표의 14일 이동평균을 기준(baseline).
	•	Z-score 대체: (오늘값 - baseline) / (표준편차+ε).
6.2 임계 규칙(예시)
	•	Sleep Low: sleep_hours <= baseline - 1.5h → 점수 +1
	•	Mood Low: mood <= 4 → +1
	•	Stress High: stress >= 7 → +1
	•	Streak: 위 규칙 발생이 연속 3일 이상 → +1 (추가)
위험 단계
	•	0~1: mild
	•	2~3: moderate → 조심스런 도움 제안
	•	≥4: severe → 현실적 조치(상담·병원·학교 상담실·국번없이 1393 등 지역 자원) 선택지 안내
실제 번호/기관 노출은 지역 설정에 따라 서버에서 삽입(모델이 생성하지 않음).

7) 안전 가이드(문구 템플릿)
	•	Moderate 예: “요즘 유난히 버거웠다면, 가까운 상담센터에 가볍게 문의해 보는 것도 한 방법이에요.”
	•	Severe 예: “도움이 급히 필요하면 지금 가까운 사람이나 전문기관에 연락해요. (앱: 도움 받기) 버튼에서 안내를 볼 수 있어요.”
프론트는 Severe 시 하단 바 우측에 ‘도움 받기’ 버튼 동시 노출(지역 리소스 페이지로 딥링크).

8) API 설계(백엔드)
8.1 생성
POST /advice/generate
Body: { userId }
동작: 서버가 사용자 컨텍스트 수집→요약→프롬프트 호출→응답 저장→최신값 반환
제한: user당 하루 기본 1회(+트리거 예외 1회), rate limit 5/min
8.2 조회
GET /advice/latest?userId=…
응답: { advice, createdAt, risk_flag }
캐시: 24h TTL(일기 제출 또는 위험 스파이크 시 무효화)
8.3 실패 대비
	•	모델/네트워크 실패 시 페일세이프 문구 10종 중 랜덤 1개 제공(로그에 원인 기록).

9) 요약 파이프라인(프라이버시)
	1.	원문 일기 → 문장 수 2~3개, 150~200자 요약(서버 내 처리).
	2.	실명·위치·연락처 등 PII 마스킹.
	3.	감정·행동 신호만 남겨 프롬프트에 투입.

10) 품질 개선 루프
	•	하단 바에 👍/👎 피드백.
	•	서버는 (컨텍스트 스냅샷, 응답, 피드백) 로깅 → A/B 프롬프트 실험.
	•	지표: 클릭률, 일기 작성 지속률, 다음날 앱 재방문율.

11) 예시 문구(상황별)
	•	일반: “해야 할 일 많아도, 오늘은 가장 작은 것 하나만 끝내요—그게 흐름을 만듭니다.”
	•	수면 부족: “밤이 짧았다면 오늘은 스스로에게 관대하게—잠깐의 낮잠도 충분히 가치 있어요.”
	•	스트레스↑: “머리가 복잡하면 3분만 타이머 맞추고 호흡에만 집중해요. 생각은 잠시 뒤로.”
	•	운동 필요: “완벽한 운동 말고 5분 스트레칭으로 몸을 깨워보세요.”
	•	관계 피로: “답장을 미뤄도 괜찮아요—당신의 속도를 존중해도 됩니다.”
	•	성과 압박: “비교 대신 기록—오늘 당신이 한 한 가지를 적어 보세요.”
	•	Moderate 플래그: “혼자 버티지 말고, 가볍게 상담을 ‘시도’해 보는 것도 충분히 용기예요.”
	•	Severe 플래그: “지금 힘이 크다면 즉시 가까운 사람/전문가에게 연락해요—당신의 안전이 최우선입니다.”

12) 프론트 UX 스펙(하단 바)
	•	기본: 좌측 아이콘(전구) + 텍스트(한 줄) + 우측 ‘새로고침’(회전 아이콘)
	•	Severe 시: 우측에 ‘도움 받기’ 보조 버튼 동시 노출
	•	텍스트 길이: 최대 1줄(작은 기기 2줄 래핑), 말줄임표 처리
	•	진입 애니메이션: 페이드인 150ms

13) 서버 의사코드
ctx = build_context(userId)
summary = summarize_diary(ctx.diary)  # 150~200자, PII mask
signals = eval_signals(ctx, baselines=14d)
risk = compute_risk(signals)
resp = call_gpt(system, dev, user_payload(ctx, summary, signals))
json = safe_parse(resp)
advice = json.advice
store_latest(userId, advice, risk, tags=json.tags)
return {advice, risk}

14) 로컬라이징 & 옵션
	•	기본 언어: 한국어(ko-KR).
	•	사용자가 영어 UI 선택 시 동일 규칙으로 영문 생성.

15) 체크리스트(개발)
	•	요약기(PII 마스킹) 구현
	•	14일 기준선·스택 계산
	•	프롬프트/응답 JSON 스키마 검증
	•	캐시/무효화 정책
	•	실패 시 페일세이프 문구 10종
	•	피드백 로깅·A/B 실험 훅
	•	지역 도움 리소스(상담·긴급 연락) DB
페일세이프 예시:
- “작게 시작해도 충분해요. 오늘은 5분만 써보는 건 어때요?”
- “하루에 한 번, 나에게 친절한 말을 건네보세요.”
- “완벽보다 지속—작은 걸음이 내일을 바꿔요.”

16) 보안·프라이버시 메모
	•	최소 데이터 원칙, 가명화 토큰 사용.
	•	일기 원문 서버 내 암호화 저장, 외부 호출 금지.
	•	프롬프트/응답 로그는 30일 롤링 보관 후 익명 통계만 유지.

끝. 다음 수정 아이디어
	•	지표 가중치 튜닝(사용자별 가중치 러닝)
	•	‘오늘의 한마디’ 클릭 시 근거 태그 미리보기(서버 생성, 프론트 선택적 노출)
	•	주간 리캡 카드(일요일 저녁 자동 생성)

필수 3축 기반 조언 로직 & 프롬프트 (v2)
축: 일기 키워드 분석, 감정 지표 하락, 반복된 감정 패턴만으로 하루 1문장 조언 생성. 대상 데이터는 어제(전일) 기록 + (패턴 판단용) 최근 3일 감정 로그.
A) 입력 스키마(백엔드 → 모델)
{
  "yesterday": {
    "diary_summary": "전일 일기 150~200자 요약(PII 마스킹)",
    "keywords": [{"token":"교수님 피드백","type":"person/event","count":1}],
    "emotions": {"mood": 5, "stress": 7, "energy": 4, "sleep_hours": 5.5}
  },
  "baselines_14d": {"mood": 6.5, "stress": 4.0, "energy": 5.8, "sleep_hours": 7.0},
  "streaks_3d": {"low_mood_days": 0, "high_stress_days": 2, "low_sleep_days": 1},
  "locale": "ko-KR"
}
B) 판단 로직(서버)
순서대로 평가하여 최우선 1개 규칙만 적용 → 1문장 생성.
	1.	감정 지표 하락(회복 우선)
	•	Sleep Low: sleep_hours <= baseline-1.5h
	•	Mood Low: mood <= 4 또는 mood <= baseline-1
	•	Stress High: stress >= 7 또는 stress >= baseline+2
	•	Energy Low: energy <= baseline-1 ⇒ 매칭 시 회복/자기돌봄 조언 카테고리 선택, risk_flag = mild|moderate(규칙 매칭 수·강도에 따라)
	1.	반복된 감정 패턴(전환 권장)
	•	동일 부정 감정 신호가 최근 3일 중 2일 이상 ⇒ 작은 변화/기분 전환 조언 카테고리 선택
	1.	일기 키워드 기반(성찰·실천)
	•	어제 일기 keywords 존재 시 ⇒ 떠올리기/한 가지 실천 제안 카테고리 선택
예외: 여러 규칙 동시 충족 시 우선순위 1 > 2 > 3. (회복이 최우선)
C) 조언 생성 가이드(카테고리별 템플릿)
	•	회복/자기돌봄(Recovery)
	•	수면: "어제 수면이 부족했죠—오늘은 평소보다 조금 일찍 눕는 걸 목표로 해봐요."
	•	스트레스: "어제 긴장이 컸다면 3분만 호흡에 집중해요—생각은 잠시 쉬어도 괜찮아요."
	•	에너지: "기운이 달렸다면 따뜻한 음료와 5분 스트레칭으로 부드럽게 시작해봐요."
	•	전환/작은 변화(Shift)
	•	"비슷한 기분이 이어졌다면 오늘은 루틴에 작은 변화를—짧은 산책이나 음악 한 곡 어떨까요?"
	•	성찰/실천(Reflect)
	•	"어제의 [키워드]가 마음에 남았죠—그중 하나만 오늘 작은 행동으로 이어가볼까요?"
D) 모델 프롬프트
System
당신은 멘탈 웰빙 코치입니다. 한국어로 **한 문장(최대 120자)**만 답하세요.
- 제안형·따뜻한 톤, 진단/치료 단정 금지.
- 이모지는 최대 1개, 문장 끝에만.
- 출력은 반드시 JSON: {"advice":"…","tags":[…],"risk_flag":"none|mild|moderate|severe"}
Developer
규칙:
1) 한 문장 120자 이내.
2) 데이터 노출 금지(예: "당신은 INFJ" 언급 금지).
3) 카테고리 선택 우선순위: 회복 > 전환 > 성찰.
4) 회복 카테고리일 때는 오늘 바로 가능한 **작은 행동 1개**만 제안.
5) 금지어: 병명, 약물, 위기 과장.
6) tags 후보: ["수면","스트레스","에너지","전환","성찰","루틴","호흡","산책","음악"]
User (동적 페이로드)
{
  "yesterday": {"diary_summary": "…", "keywords": [ … ], "emotions": {"mood": 5, "stress": 7, "energy": 4, "sleep_hours": 5.5}},
  "baselines_14d": {"mood": 6.5, "stress": 4.0, "energy": 5.8, "sleep_hours": 7.0},
  "streaks_3d": {"high_stress_days": 2, "low_sleep_days": 1},
  "policy": {"max_chars": 120, "locale": "ko-KR"}
}
E) 모델 출력 예시
{"advice":"어제 긴장이 컸다면 3분만 호흡에 몰입해요—생각은 잠시 쉬어도 괜찮아요.",
 "tags":["스트레스","호흡","전환"],
 "risk_flag":"mild"}
F) 실패 대비 문구(백엔드 페일세이프)
	•	"작게 시작해도 좋아요—오늘은 5분만 쉬어가요."
	•	"완벽보다 지속—작은 걸음이 내일을 바꿉니다."
	•	"스스로에게 부드럽게—오늘 한 가지를 덜어내도 괜찮아요."
G) 테스트 케이스(샘플 입력 → 기대 출력)
	1.	stress=8, baseline=4 → 회복/호흡 조언 + risk=mild/moderate
	2.	mood=4, baseline=6 → 회복/수면·자기연민 조언
	3.	3일 연속 high_stress_days=2 이상 → 전환(산책/음악)
	4.	키워드=["교수님 피드백"]만 존재 → 성찰/실천 조언


7.프로필 설정 활동지수 옆에 멘탈지수 추가
감정상태와 태그들을 한달기준으로 수치화 

추가 지표: 클릭률, 일기 작성 지속률, 다음날 앱 재방문율.